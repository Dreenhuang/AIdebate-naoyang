# UI Design - PRD Debate Dashboard (taolun-web)

## 项目信息
- **项目名称**: PRD Debate Dashboard (taolun-web)
- **版本**: v2.0
- **创建日期**: 2026-05-08
- **关联文档**: [Product-Spec.md](./Product-Spec.md), [Design-Brief.md](./Design-Brief.md)

---

## 1. 设计系统

### 1.1 色彩系统

```css
/* 背景色 */
--bg-primary: #0F1117;      /* 主背景 */
--bg-secondary: #1A1D24;    /* 卡片背景 */
--bg-tertiary: #252830;     /* 输入框背景 */
--bg-hover: #2A2E3A;        /* hover状态 */

/* 文字色 */
--text-primary: #F1F5F9;    /* 主文字 */
--text-secondary: #94A3B8;  /* 次要文字 */
--text-muted: #64748B;      /* 弱化文字 */

/* 品牌色 */
--brand-primary: #3B82F6;   /* 主色 - 蓝 */
--brand-secondary: #10B981; /* 辅助色 - 绿 */
--brand-accent: #F59E0B;    /* 强调色 - 琥珀 */

/* 语义色 */
--success: #10B981;
--warning: #F59E0B;
--error: #EF4444;
--info: #3B82F6;
--processing: #8B5CF6;

/* 角色色 */
--role-host: #3B82F6;       /* 主持人 - 蓝 */
--role-proposer: #10B981;   /* 提案者 - 绿 */
--role-reviewer: #F97316;   /* 审查者 - 橙 */
--role-system: #6B7280;     /* 系统 - 灰 */

/* 边框 */
--border-primary: #2A2E3A;
--border-focus: #3B82F6;
```

### 1.2 字体系统

```css
/* 字体族 */
--font-primary: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
--font-mono: 'JetBrains Mono', 'Fira Code', monospace;

/* 字号 */
--text-xs: 12px;
--text-sm: 14px;
--text-base: 16px;
--text-lg: 18px;
--text-xl: 24px;
--text-2xl: 32px;

/* 字重 */
--font-normal: 400;
--font-medium: 500;
--font-semibold: 600;
--font-bold: 700;

/* 行高 */
--leading-tight: 1.2;
--leading-normal: 1.5;
--leading-relaxed: 1.75;
```

### 1.3 间距系统

```css
/* 基础单位: 4px */
--space-1: 4px;
--space-2: 8px;
--space-3: 12px;
--space-4: 16px;
--space-5: 20px;
--space-6: 24px;
--space-8: 32px;
--space-10: 40px;

/* 圆角 */
--radius-sm: 4px;
--radius-md: 8px;
--radius-lg: 12px;
--radius-xl: 16px;
--radius-full: 9999px;
```

### 1.4 阴影系统

```css
/* 微阴影 */
--shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.3);
/* 卡片阴影 */
--shadow-md: 0 4px 6px rgba(0, 0, 0, 0.4);
/* 弹窗阴影 */
--shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.5);
```

---

## 2. 组件库

### 2.1 按钮 (Button)

**主要按钮 (Primary)**
- 背景: --brand-primary
- 文字: 白色
- 圆角: --radius-md
- 内边距: 10px 16px
- hover: 亮度提升 10%
- active: 亮度降低 10%

**次要按钮 (Secondary)**
- 背景: --bg-tertiary
- 文字: --text-primary
- 边框: 1px solid --border-primary
- hover: 背景变为 --bg-hover

**危险按钮 (Danger)**
- 背景: --error
- 文字: 白色

**文字按钮 (Text)**
- 背景: 透明
- 文字: --brand-primary
- hover: 文字下划线

**加载状态**
- 显示旋转图标，禁用点击

### 2.2 输入框 (Input)

**文本输入**
- 背景: --bg-tertiary
- 边框: 1px solid --border-primary
- 圆角: --radius-md
- 内边距: 10px 12px
- 文字: --text-primary
- placeholder: --text-muted
- focus: 边框变为 --border-focus，外发光

**文本域 (Textarea)**
- 同文本输入，高度自适应
- 最小高度: 80px

**选择框 (Select)**
- 同文本输入样式
- 下拉箭头图标

### 2.3 卡片 (Card)

**标准卡片**
- 背景: --bg-secondary
- 圆角: --radius-lg
- 内边距: --space-4
- 边框: 1px solid --border-primary

**可折叠卡片**
- 头部: 标题 + 展开/折叠图标
- 内容: 默认展开/折叠

### 2.4 标签/徽章 (Badge)

**状态标签**
- 圆角: --radius-full
- 内边距: 4px 10px
- 小号字体
- 背景色根据状态变化

**角色标签**
- 左侧彩色圆点
- 右侧角色名称
- 背景: 半透明角色色

### 2.5 消息气泡 (Message Bubble)

**布局**
- 左侧: 角色头像 (32px圆形)
- 右侧: 消息内容
- 上下间距: --space-3

**头像**
- 圆形，角色色背景
- 角色首字母

**内容区**
- 顶部: 角色名 + 时间戳
- 中部: 消息文本
- 底部: 阶段/轮次标签

### 2.6 步骤条 (Stepper)

**水平步骤条**
- 步骤圆点: 24px，角色色
- 步骤线: 2px，完成部分角色色，未完成部分灰色
- 步骤标签: 下方文字

**垂直步骤条**
- 用于阶段展示
- 左侧时间线，右侧内容

### 2.7 进度指示器

**圆形进度**
- 用于承诺计数
- 外圈: 总承诺数
- 内圈: 已完成数

**线性进度**
- 用于阶段进度
- 高度: 4px
- 完成部分: 角色色

### 2.8 开关 (Toggle)

- 背景: --bg-tertiary
- 滑块: 白色圆形
- 开启: 背景变为 --brand-primary
- 动画: 滑块滑动 200ms

### 2.9 滑块 (Slider)

- 轨道: 灰色背景条
- 滑块: 白色圆形，带阴影
- 数值标签: 上方显示当前值

### 2.10 文件列表项

**布局**
- 左侧: 文件类型图标
- 中部: 文件名 + 大小
- 右侧: 操作按钮 (预览/下载)

**选中状态**
- 背景: --bg-hover
- 左侧边框: 3px solid --brand-primary

---

## 3. 页面设计

### 3.1 Dashboard 主页面

#### 布局结构

```
┌─────────────────────────────────────────────────────────────┐
│ Header (60px)                                               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Logo + "PRD Debate Dashboard"          连接状态 [●]     │ │
│ └─────────────────────────────────────────────────────────┘ │
├──────────────┬──────────────────────────────────────────────┤
│              │                                              │
│ Sidebar      │  Main Content                                │
│ (320px)      │                                              │
│              │  ┌────────────────────────────────────────┐  │
│ 配置面板      │  │ Message Stream                         │  │
│ ┌──────────┐ │  │                                        │  │
│ │ 话题输入  │ │  │  ┌────┐ ┌─────────────────────────┐  │  │
│ │          │ │  │  │ 👤 │ │ 主持人                  │  │  │
│ ├──────────┤ │  │  │    │ │ 欢迎来到第1阶段讨论...   │  │  │
│ │ 角色配置  │ │  │  └────┘ └─────────────────────────┘  │  │
│ │ ┌──────┐ │ │  │                                        │  │
│ │ │角色1 │ │ │  │  ┌────┐ ┌─────────────────────────┐  │  │
│ │ │角色2 │ │ │  │  │ 👤 │ │ 提案者                  │  │  │
│ │ │角色3 │ │ │  │  │    │ │ 我认为这个方案...        │  │  │
│ │ └──────┘ │ │  │  └────┘ └─────────────────────────┘  │  │
│ ├──────────┤ │  │                                        │  │
│ │ 轮数设置  │ │  │  ...                                   │  │
│ │ [====●=] │ │  │                                        │  │
│ ├──────────┤ │  └────────────────────────────────────────┘  │
│ │ 阶段设置  │ │                                              │
│ │ [====●=] │ │                                              │
│ ├──────────┤ │                                              │
│ │ 控制按钮  │ │                                              │
│ │ [开始]   │ │                                              │
│ │ [停止]   │ │                                              │
│ └──────────┘ │                                              │
│              │                                              │
├──────────────┴──────────────────────────────────────────────┤
│ Status Bar (80px)                                           │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 阶段: 2/5 │ 轮次: 3/5 │ 承诺: 12 │ 回溯: ✓ │ 状态: 🟢  │ │
│ └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│ File Manager (200px, 可折叠)                                │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 📁 文件列表                    [批量下载] [全部下载]    │ │
│ │ ☑ debate-framework.md                    [下载] [预览]  │ │
│ │ ☑ consensus.md                           [下载] [预览]  │ │
│ │ ☑ prd.md                                 [下载] [预览]  │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

#### Header 设计

- **高度**: 60px
- **背景**: --bg-secondary
- **底部边框**: 1px solid --border-primary
- **左侧**: Logo (32px图标 + "taolun" 文字)
- **右侧**: 连接状态指示器
  - 已连接: 绿色脉冲圆点 + "已连接"
  - 断开: 红色圆点 + "已断开"
  - 重连中: 琥珀色旋转图标 + "重连中(3s)"

#### Sidebar 配置面板

- **宽度**: 320px
- **背景**: --bg-secondary
- **右侧边框**: 1px solid --border-primary
- **内边距**: --space-4

**话题输入区**
- 标签: "讨论话题"
- 输入框: Textarea，placeholder "输入讨论话题..."
- 示例: "AI会取代人类的工作吗？"

**角色配置区**
- 标签: "角色配置 (3/5)"
- 角色数量滑块: 2-5
- 每个角色卡片:
  - 折叠/展开按钮
  - 角色名称输入
  - 模型选择 (Select: deepseek-v4-flash / MiniMax)
  - Soul描述 (Textarea)
  - 删除按钮 (hover显示)

**轮数设置**
- 标签: "每阶段轮数"
- 滑块: 1-10，默认值5
- 数值显示: 右侧数字

**阶段设置**
- 标签: "总阶段数"
- 滑块: 1-5，默认值5
- 数值显示: 右侧数字

**控制按钮区**
- 开始按钮: 主要按钮，全宽
- 停止按钮: 危险按钮，全宽
- 重置按钮: 文字按钮

#### Main Content 消息流

- **背景**: --bg-primary
- **内边距**: --space-4
- **滚动**: 垂直滚动，自动滚动到底部

**消息项**
- 布局: Flex，左侧头像，右侧内容
- 头像: 32px圆形，角色色背景，首字母
- 内容区:
  - 头部: 角色名 (粗体) + 时间戳 (小号灰色)
  - 消息文本: 14px，--text-primary
  - 标签: 阶段X · 轮次Y (Badge样式)
- 间距: 消息之间 --space-4

**系统消息**
- 居中显示
- 灰色文字
- 无头像

**流式输出效果**
- 打字机效果，逐字显示
- 光标闪烁

#### Status Bar 状态栏

- **高度**: 80px
- **背景**: --bg-secondary
- **顶部边框**: 1px solid --border-primary
- **内边距**: 0 --space-6
- **布局**: Flex，space-between

**状态项**
- 阶段进度: "阶段 2/5" + 步骤条
- 轮次: "轮次 3/5"
- 承诺计数: 大数字 "12" + "承诺"
- 回溯状态: 图标 + "通过" / "未通过"
- 整体状态: 彩色圆点 + 状态文字

#### File Manager 文件管理

- **高度**: 200px (可折叠)
- **背景**: --bg-secondary
- **顶部边框**: 1px solid --border-primary

**头部**
- 左侧: "📁 文件列表 (6)"
- 右侧: 批量操作按钮

**文件列表**
- 每项: 复选框 + 文件图标 + 文件名 + 大小 + 操作按钮
- hover: 背景变为 --bg-hover
- 选中: 左侧蓝色边框

### 3.2 配置面板弹窗 (移动端)

- **全屏弹窗**
- **头部**: 标题 + 关闭按钮
- **内容**: 同Sidebar配置面板
- **底部**: 保存按钮

### 3.3 文件预览弹窗

- **弹窗**: 居中，宽度 800px，高度 600px
- **头部**: 文件名 + 关闭按钮 + 下载按钮
- **内容**: Markdown渲染
- **背景**: 半透明遮罩

---

## 4. 交互流程

### 4.1 启动辩论流程

```
[用户填写配置] → [点击开始按钮]
    ↓
[按钮变为加载状态] → [发送WebSocket消息: debate:start]
    ↓
[等待后端响应] → [收到 debate:started]
    ↓
[按钮变为停止] → [状态栏更新] → [消息流开始接收消息]
```

### 4.2 接收消息流程

```
[WebSocket接收 debate:message]
    ↓
[解析消息: 角色、阶段、轮次、内容]
    ↓
[添加到消息列表] → [自动滚动到底部]
    ↓
[更新状态栏: 阶段/轮次]
```

### 4.3 文件下载流程

```
[用户点击下载按钮]
    ↓
[调用API: GET /api/files/download?files=xxx.md]
    ↓
[显示下载进度] → [完成下载]
```

### 4.4 WebSocket重连流程

```
[检测到连接断开]
    ↓
[显示重连状态] → [开始倒计时]
    ↓
[尝试重连] → [成功/失败]
    ↓
[恢复连接] / [继续重试]
```

---

## 5. 状态变体

### 5.1 Dashboard 页面状态

| 状态 | 描述 | 视觉表现 |
|------|------|---------|
| 默认态 | 初始加载完成 | 正常显示所有区域 |
| 空状态 | 无辩论进行 | 消息流显示"等待辩论开始" |
| 加载态 | 正在连接WebSocket | 连接状态显示"连接中..." |
| 运行态 | 辩论进行中 | 消息流实时更新，状态栏活跃 |
| 完成态 | 辩论已完成 | 显示完成提示，文件列表可下载 |
| 错误态 | 连接失败 | 显示错误提示，重连按钮 |

### 5.2 消息流状态

| 状态 | 描述 | 视觉表现 |
|------|------|---------|
| 默认态 | 正常显示消息 | 消息按时间顺序排列 |
| 流式态 | 正在接收消息 | 消息逐字显示，光标闪烁 |
| 空状态 | 无消息 | 显示提示文字和开始按钮 |

### 5.3 文件管理状态

| 状态 | 描述 | 视觉表现 |
|------|------|---------|
| 默认态 | 文件列表 | 正常显示文件项 |
| 空状态 | 无文件 | 显示"暂无文件"提示 |
| 加载态 | 正在加载文件 | 骨架屏或旋转图标 |
| 选中态 | 文件被选中 | 高亮背景，左侧边框 |

---

## 6. 响应式设计

### 6.1 桌面端 (>= 1280px)

- 左侧Sidebar固定显示
- 主区域自适应宽度
- 底部状态栏和文件管理固定高度

### 6.2 平板端 (768px - 1279px)

- Sidebar可折叠为图标栏 (60px)
- 点击图标展开Sidebar
- 主区域全宽

### 6.3 手机端 (< 768px)

- 隐藏Sidebar
- 底部Tab导航: 配置 / 消息 / 文件
- 配置面板为全屏弹窗
- 消息流全屏
- 文件管理为底部弹窗

---

## 7. 动画规范

### 7.1 页面加载

- 淡入: opacity 0 → 1, 200ms, ease-out
- 延迟: 各区域依次加载，间隔 50ms

### 7.2 消息出现

- 滑入: translateY(10px) → translateY(0), 150ms, ease-out
- 淡入: opacity 0 → 1, 150ms

### 7.3 状态变化

- 颜色过渡: 300ms, ease
- 数字变化: 计数动画, 300ms

### 7.4 按钮交互

- hover: 亮度变化, 150ms
- active: scale(0.98), 100ms
- 加载: 旋转动画, 1s linear infinite

### 7.5 弹窗

- 打开: scale(0.95) → scale(1), opacity 0 → 1, 200ms
- 关闭: 反向动画
- 遮罩: opacity 0 → 0.5, 200ms

---

## 8. 图标系统

### 8.1 图标库
- **Lucide React**: 主要图标库
- **自定义图标**: Logo、角色头像

### 8.2 图标列表

| 图标 | 用途 | 名称 |
|------|------|------|
| ⚡ | Logo | Zap |
| 👤 | 用户头像 | User |
| 🤖 | AI角色 | Bot |
| 🎯 | 话题 | Target |
| ⚙️ | 配置 | Settings |
| ▶️ | 开始 | Play |
| ⏹️ | 停止 | Square |
| 🔄 | 重置 | RotateCcw |
| 📁 | 文件 | Folder |
| 📄 | 文档 | FileText |
| ⬇️ | 下载 | Download |
| 👁️ | 预览 | Eye |
| ☑️ | 选中 | CheckSquare |
| ✅ | 完成 | Check |
| ❌ | 错误 | X |
| ⚠️ | 警告 | AlertTriangle |
| 🔗 | 连接 | Link |
| 🔴 | 断开 | Unlink |
| 📊 | 统计 | BarChart |
| 💬 | 消息 | MessageSquare |

---

## 9. 设计原则检查清单

- [x] 深色主题，减少视觉疲劳
- [x] 色彩语义化，状态清晰
- [x] 信息密度高但层次分明
- [x] 专业冷静，避免过度装饰
- [x] 实时反馈，状态变化即时
- [x] 角色区分，不同颜色标识
- [x] 响应式设计，多端适配
- [x] 动画适度，不干扰使用

---

**下一步**: 
- 调用 dev-planner 制定开发计划
- 或开始编码实现
