# 多AI讨论技能 - 安装与使用说明

## 一、技能简介

"多AI讨论"是一款基于PRD辩论框架的智能讨论系统，支持多角色AI协同、实时可视化辩论进度、灵活的Soul灵魂设定系统。

**全局触发关键字**: `多AI讨论`

---

## 二、安装步骤

### 2.1 克隆项目到本地

在终端或命令行中执行以下命令：

```bash
# 克隆项目仓库
git clone git@github.com:betseyliu/prd-debate.git prd-debate

# 进入项目目录
cd prd-debate

# 查看项目结构
ls -la
```

### 2.2 安装依赖

#### 前端依赖安装

```bash
cd taolun-web/frontend

# 使用 bun 安装前端依赖（推荐）
bun install

# 或使用 npm
npm install
```

#### 后端依赖安装

```bash
cd taolun-web/backend

# 使用 bun 安装后端依赖（推荐）
bun install

# 或使用 npm
npm install
```

### 2.3 配置环境变量

在 `taolun-web/backend` 目录下创建 `.env` 文件：

```bash
# 复制示例环境变量文件
cp .env.example .env
```

编辑 `.env` 文件，配置API密钥：

```env
# 服务器配置
PORT=9528
NODE_ENV=development

# DeepSeek API配置
DEEPSEEK_API_KEY=your-deepseek-api-key-here

# MiniMax API配置
MINIMAX_API_KEY=your-minimax-api-key-here

# 前端URL（开发环境）
FRONTEND_URL=http://localhost:9516
```

### 2.4 验证安装

#### 启动后端服务

```bash
cd taolun-web/backend
bun run start
# 或
node src/index.js
```

后端启动后应该看到：
```
[Server] Backend running on port 9528
[WebSocket] Server initialized
```

#### 启动前端服务

```bash
cd taolun-web/frontend
bun run dev
# 或
npm run dev
```

前端启动后应该看到：
```
VITE ready in xxx ms
➜ Local: http://localhost:9516/
```

#### 访问系统

打开浏览器访问：http://localhost:9516

---

## 三、使用方法

### 3.1 一键启动（推荐）

直接双击项目根目录下的 `AI讨论-trae-kimi启动.bat` 文件，系统将自动：
1. 检测并清理占用端口的进程
2. 启动后端服务（端口9528）
3. 启动前端服务（端口9516）
4. 自动打开浏览器访问系统

### 3.2 创建桌面快捷方式

```powershell
# 运行创建快捷方式脚本
powershell -ExecutionPolicy Bypass -File create-shortcut.ps1
```

或在PowerShell中直接运行：
```powershell
$WshShell = New-Object -comObject WScript.Shell
$Shortcut = $WshShell.CreateShortcut("$env:USERPROFILE\Desktop\多AI讨论.lnk")
$Shortcut.TargetPath = "%~dp0AI讨论-trae-kimi启动.bat"
$Shortcut.WorkingDirectory = "%~dp0"
$Shortcut.IconLocation = "%SystemRoot%\System32\SHELL32.dll, 14"
$Shortcut.Description = "启动多AI讨论系统"
$Shortcut.Save()
```

---

## 四、核心功能使用

### 4.1 配置辩论话题

在左侧配置面板的"讨论话题"输入框中输入您想要讨论的话题，例如：
- "AI会取代人类的工作吗？"
- "远程办公是否优于办公室办公？"
- "自动驾驶的道德困境如何解决？"

### 4.2 配置讨论角色

系统默认提供三种角色：
- **主持人（Host）**：引导讨论方向，保持中立
- **提案者（Proposer）**：提出方案和观点
- **审查者（Reviewer）**：质疑和审查提案

每个角色可以：
- 自定义角色名称
- 选择AI模型（DeepSeek V4 Flash / MiniMax）
- 配置Soul灵魂设定（性格、思维方式、价值观等）

### 4.3 选择Soul预设

系统为每种角色提供了8套精心设计的Soul预设：

**主持人Soul模板：**
1. 苏格拉底式引导者 - 提问引导深度思考
2. 高效执行推动者 - 聚焦目标结果导向
3. 外交平衡协调者 - 冲突调解多方共赢
4. 挑战者 - 批判思维逆向思考
5. 系统整合架构师 - 系统思维模式识别
6. 叙事编织者 - 故事引导情感共鸣
7. 数据驱动分析师 - 客观证据导向
8. 未来场景构建者 - 可能性探索引导

**提案者Soul模板：**
1. 远见创新梦想家 - 突破性创新愿景
2. 务实方案工匠 - 渐进迭代落地
3. 用户共情设计师 - 用户中心视角
4. 技术趋势前瞻者 - 技术可行性分析
5. 商业模式架构师 - 商业价值创造
6. 生态平台构建者 - 网络效应设计
7. 极简主义设计师 - 本质主义减法
8. 社会创新倡导者 - 可持续发展视角

**审查者Soul模板：**
1. 严谨逻辑审查官 - 逻辑漏洞识别
2. 风险预警守护者 - 风险预防思维
3. 伦理价值守门人 - 社会责任审视
4. 用户体验挑剔者 - 体验细节把关
5. 系统思维分析师 - 长期影响评估
6. 安全隐私守护者 - 数据安全保障
7. 文化敏感度审查者 - 跨文化适用性
8. 无障碍包容性审查者 - 普惠设计验证

### 4.4 自定义Soul

点击角色卡片的"管理"按钮可以：
- 查看所有预设Soul
- 创建自定义Soul设定
- 编辑现有Soul内容
- 删除不需要的Soul

### 4.5 随机分配Soul

点击"随机Soul"按钮，系统将为所有角色随机分配Soul预设，省去手动选择的麻烦。

### 4.6 设置辩论参数

- **每阶段轮数**：1-10轮，默认5轮
- **总阶段数**：1-5阶段，默认5阶段

### 4.7 开始辩论

配置完成后，点击"开始辩论"按钮，系统将：
1. 显示连接状态（已连接/重连中/已断开）
2. 实时显示辩论进度（当前阶段/轮次）
3. 展示各角色的发言内容
4. 统计承诺数量
5. 支持实时消息流滚动

### 4.8 文件管理

辩论完成后，可以在底部文件管理器：
- 查看生成的文件列表
- 选择性下载文件（多选）
- 全部下载功能
- 文件预览

---

## 五、API接口说明

### 5.1 健康检查

```
GET /api/health
```

响应：
```json
{
  "status": "ok",
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

### 5.2 获取辩论状态

```
GET /api/debate/state
```

### 5.3 获取消息列表

```
GET /api/debate/messages
```

### 5.4 Soul管理接口

```
GET /api/souls                    # 获取所有自定义Soul
GET /api/souls/:roleType          # 按角色类型获取Soul
POST /api/souls/:roleType         # 添加自定义Soul
PUT /api/souls/:roleType/:soulId  # 更新Soul
DELETE /api/souls/:roleType/:soulId # 删除Soul
```

---

## 六、WebSocket通信

### 6.1 连接地址

```
ws://localhost:9528
```

### 6.2 消息类型

**客户端发送：**
- `debate:start` - 开始辩论（携带配置参数）
- `debate:stop` - 停止辩论
- `debate:reset` - 重置辩论

**服务端推送：**
- `system:connected` - 连接成功
- `debate:started` - 辩论开始
- `debate:phase` - 阶段更新
- `debate:round` - 轮次更新
- `debate:message` - 新消息
- `debate:commitment` - 承诺更新
- `debate:completed` - 辩论完成
- `debate:stopped` - 辩论停止

---

## 七、常见问题

### Q1: 启动后显示"服务器未连接"

**解决方案：**
1. 确保后端服务已启动（端口9528）
2. 检查防火墙设置
3. 确认.env文件中的API密钥配置正确

### Q2: 前端页面空白或样式异常

**解决方案：**
1. 清除浏览器缓存
2. 检查前端依赖是否完整安装
3. 尝试重新安装依赖：`rm -rf node_modules && npm install`

### Q3: WebSocket连接失败

**解决方案：**
1. 检查端口是否被占用
2. 确认后端服务正常运行
3. 检查浏览器控制台错误信息

### Q4: AI模型调用失败

**解决方案：**
1. 确认API密钥有效且额度充足
2. 检查网络连接
3. 查看后端日志获取详细错误信息

---

## 八、技术架构

### 8.1 前端技术栈
- React 18
- Vite
- TailwindCSS
- Zustand（状态管理）
- Lucide React（图标）
- WebSocket（实时通信）

### 8.2 后端技术栈
- Node.js
- Express
- WebSocket (ws)
- Multer（文件处理）
- Archiver（压缩打包）

### 8.3 端口配置
- 前端开发服务器：9516
- 后端服务器：9528
- WebSocket：与后端共用9528端口

---

## 九、联系方式

如有问题或建议，请通过以下方式联系：
- GitHub Issues: https://github.com/betseyliu/prd-debate/issues
- 邮箱：support@example.com

---

**版本**: v2.0.0
**更新日期**: 2024年
**许可证**: MIT License
