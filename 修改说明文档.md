# Taolun 辩论系统 - 修改说明文档

**修改日期**: 2026年05月08日  
**修改版本**: v2.2.0 (更新)  
**修改人**: AI Assistant (Kimi K2.6)

---

## 一、变更概述

本次修改主要完成了以下工作：

1. **迁移 Debate Skill 核心框架**：将位于 `G:\ai-gongju\prd-debate\debate` 的 skill 核心原理、逻辑和设计思想系统性应用到当前项目中
2. **修复 WebSocket 通信问题**：修复了点击"开始辩论"按钮无响应的问题
3. **新增辩论阶段指示器**：可视化展示辩论进行的阶段进度
4. **新增共识面板**：展示辩论过程中的阶段共识和回溯校验结果
5. **修复数据类型问题**：确保阶段和轮次数据以数字类型传递，避免渲染错误
6. **完善回溯校验逻辑**：实现完整的对齐检查、范围检查、优先级检查、一致性检查和可行性检查
7. **添加导出功能**：支持将辩论报告导出为 Markdown 和 HTML（可打印为 PDF）格式
8. **优化 UI/UX 界面设计**：增强共识面板的交互性和信息展示
9. **添加单元测试和集成测试**：为后端引擎和前端状态管理添加完整的测试用例

---

## 二、详细变更内容

### 2.1 后端变更

#### 2.1.1 新增 DebateEngine 核心引擎

**文件**: `backend/src/services/debateEngine.js`

**新增内容**:
- `DebateEngine` 类：结构化对抗性辩论引擎核心实现
- `ContextManager` 类：管理辩论上下文，包括阶段上下文、提案者上下文、审查者上下文
- `BacktrackValidator` 类：回溯校验器，验证跨阶段承诺的一致性
- 定义了辩论阶段（PHASES）：需求探查、方案设计、实现规划、验证确认
- 定义了角色类型（ROLES）：主持人(host)、提案者(proposer)、审查者(reviewer)
- 定义了消息类型（MESSAGE_TYPES）：提案、审查、共识、系统、承诺

**核心功能**:
- 多阶段辩论流程管理
- 多轮次讨论控制
- 承诺提取和追踪
- 共识生成
- 回溯校验（对齐检查、范围检查、优先级检查）
- 5选3推进规则评估

#### 2.1.2 更新 WebSocket 处理器

**文件**: `backend/src/websocket/handler.js`

**变更内容**:
- 集成 `DebateEngine` 到 WebSocket 处理器
- 新增 `setupDebateEngineListeners()` 方法，监听 DebateEngine 事件并广播到客户端
- 支持的事件类型：
  - `debate:started` - 辩论开始
  - `debate:phase` - 阶段切换
  - `debate:round` - 轮次更新
  - `debate:message` - 新消息
  - `debate:consensus` - 共识生成
  - `debate:backtrack` - 回溯校验结果
  - `debate:complete` - 辩论完成
  - `debate:stopped` - 辩论停止

#### 2.1.3 更新 DebateService

**文件**: `backend/src/services/debateService.js`

**变更内容**:
- 新增 `setBroadcastCallback()` 方法，支持外部设置广播回调
- 新增 `broadcast()` 方法，将消息广播到所有客户端
- 修改 `addMessage()` 方法，添加消息时自动广播
- 修改 `simulateDebate()` 方法：
  - 添加辩论开始广播
  - 添加轮次更新广播
  - 添加阶段更新广播
  - 添加辩论完成广播
- 修复阶段/轮次计数逻辑，防止越界

### 2.2 前端变更

#### 2.2.1 更新 DebateStore

**文件**: `frontend/src/stores/debateStore.js`

**新增状态**:
- `commitments`: 承诺列表
- `consensus`: 共识列表
- `backtrackResults`: 回溯校验结果
- `debateEngine`: 辩论引擎状态
- `phases`: 阶段配置（需求探查、方案设计、实现规划、验证确认）

**新增 Actions**:
- `addCommitment()`: 添加承诺
- `setConsensus()`: 设置共识列表
- `addConsensus()`: 添加共识
- `setBacktrackResults()`: 设置回溯结果
- `addBacktrackResult()`: 添加回溯结果
- `setDebateEngine()`: 设置辩论引擎

#### 2.2.2 更新 WebSocket Hook

**文件**: `frontend/src/hooks/useWebSocket.js`

**变更内容**:
- 新增 `addConsensus` 和 `addBacktrackResult` 到 store actions
- 新增事件处理：
  - `debate:consensus` - 调用 `addConsensus()`
  - `debate:backtrack` - 调用 `addBacktrackResult()`
- 修复 `debate:phase` 事件处理：提取 `payload.phase` 数字值
- 修复 `debate:round` 事件处理：提取 `payload.round` 数字值

#### 2.2.3 新增 DebatePhaseIndicator 组件

**文件**: `frontend/src/components/DebatePhaseIndicator.jsx`

**功能**:
- 显示当前辩论阶段进度
- 展示四个阶段：需求探查、方案设计、实现规划、验证确认
- 使用不同颜色标识已完成、进行中、待进行状态
- 响应式设计，小屏幕显示数字序号

#### 2.2.4 新增 ConsensusPanel 组件

**文件**: `frontend/src/components/ConsensusPanel.jsx`

**功能**:
- 显示最新阶段共识摘要
- 展示核心承诺列表
- 显示回溯校验结果（通过/存在张力/发现矛盾）
- 根据校验状态显示不同颜色标识

#### 2.2.5 更新 App.jsx

**文件**: `frontend/src/App.jsx`

**变更内容**:
- 导入 `DebatePhaseIndicator` 和 `ConsensusPanel` 组件
- 在主内容区上方添加 `DebatePhaseIndicator`
- 在右侧添加 `ConsensusPanel` 面板
- 根据辩论状态动态显示/隐藏共识面板

---

## 三、修复的问题

### 3.1 严重问题修复

#### 问题1：点击"开始辩论"按钮页面崩溃

**现象**: 点击"开始辩论"按钮后，页面变为空白，控制台显示 "Objects are not valid as a React child"

**根因**: 
- `useWebSocket.js` 中的 `setPhase` 和 `setRound` 直接传入了整个 `payload` 对象
- `DebateEngine` 广播的 `phase` 是字符串（如 'probe'），而不是数字

**修复方案**:
- 修改 `useWebSocket.js`，提取 `payload.phase` 和 `payload.round` 的数字值
- 修改 `DebateEngine`，将 `phase` 改为数字索引，`phaseId` 保留字符串标识

#### 问题2：阶段/轮次计数越界

**现象**: 状态栏显示 "6 / 5"，超出设定的最大值

**根因**: `debateService.js` 中的阶段切换逻辑有问题，在辩论完成后未正确停止计数

**修复方案**: 重新组织 `simulateDebate()` 中的阶段切换逻辑，确保在辩论完成后立即清除定时器

### 3.2 中等问题修复

#### 问题3：消息内容显示为模板文本

**现象**: 消息流显示 "这是[角色]的第N条消息。正在讨论话题：..."

**说明**: 这是模拟数据，实际项目中需要接入 AI API 生成真实内容

#### 问题4：WebSocket 消息未广播到客户端

**现象**: 后端生成消息但前端未收到

**根因**: `debateService.js` 只是内部添加消息，没有广播机制

**修复方案**: 添加 `broadcastCallback` 机制，将消息广播到所有连接的客户端

---

## 四、影响范围

### 4.1 受影响文件

**后端**:
- `backend/src/services/debateEngine.js` (新增)
- `backend/src/websocket/handler.js` (修改)
- `backend/src/services/debateService.js` (修改)

**前端**:
- `frontend/src/stores/debateStore.js` (修改)
- `frontend/src/hooks/useWebSocket.js` (修改)
- `frontend/src/components/DebatePhaseIndicator.jsx` (新增)
- `frontend/src/components/ConsensusPanel.jsx` (新增)
- `frontend/src/App.jsx` (修改)

### 4.2 功能影响

**新增功能**:
- 结构化辩论流程（4个阶段）
- 阶段进度可视化
- 共识展示面板
- 回溯校验结果展示

**改进功能**:
- WebSocket 通信稳定性
- 辩论状态管理
- 消息广播机制

**保持兼容**:
- 原有角色配置功能
- 文档上传功能
- Soul 预设管理
- 主题切换功能

---

## 五、测试结果

### 5.1 功能测试

| 测试项 | 结果 | 说明 |
|--------|------|------|
| 页面加载 | 通过 | 正常渲染所有组件 |
| 话题输入 | 通过 | 输入正常，按钮状态更新 |
| 开始辩论 | 通过 | 辩论正常启动，无崩溃 |
| 消息流显示 | 通过 | 消息正常显示，角色轮换正确 |
| 阶段进度 | 通过 | 显示数字，无越界 |
| 轮次显示 | 通过 | 显示数字，无越界 |
| 阶段指示器 | 通过 | 显示中文阶段名称 |
| 停止辩论 | 通过 | 辩论正常停止 |
| 页面稳定性 | 通过 | 25秒持续运行无崩溃 |

### 5.2 兼容性测试

| 测试项 | 结果 | 说明 |
|--------|------|------|
| 原有角色配置 | 通过 | 角色增删改查正常 |
| 文档上传 | 通过 | 上传功能未受影响 |
| Soul 预设 | 通过 | 预设管理正常 |
| 主题切换 | 通过 | 明暗主题切换正常 |
| 响应式布局 | 通过 | 不同屏幕尺寸正常显示 |

### 5.3 性能测试

| 测试项 | 结果 | 说明 |
|--------|------|------|
| 消息生成速度 | 正常 | 2秒间隔生成消息 |
| 内存占用 | 正常 | 无内存泄漏 |
| WebSocket 连接 | 正常 | 连接稳定，自动重连 |

---

## 六、已知问题与限制

### 6.1 已知问题

1. **消息内容为模拟数据**：当前消息内容为模板文本，需要接入 AI API 生成真实内容
2. **共识生成为空**：`DebateEngine` 中的共识摘要生成需要接入 AI 服务
3. **回溯校验简化**：当前回溯校验为简化实现，完整实现需要更复杂的逻辑

### 6.2 使用限制

1. **最大阶段数**: 4 个阶段（需求探查、方案设计、实现规划、验证确认）
2. **最大角色数**: 5 个角色
3. **每阶段最大轮数**: 10 轮

---

## 七、下一步计划

1. **接入 AI API**：接入 DeepSeek 或 MiniMax API 生成真实的辩论内容
2. **完善共识生成**：使用 AI 服务生成更准确的共识摘要
3. **添加更多导出格式**：支持 Word (.docx) 和 Excel (.xlsx) 导出
4. **添加动画效果**：改进消息流和阶段切换的动画
5. **性能优化**：优化大量消息的渲染性能

---

## 八、v2.2.0 新增功能详情

### 8.1 完善回溯校验逻辑 (BacktrackValidator)

**文件**: `backend/src/services/debateEngine.js` - BacktrackValidator 类

**新增检查类型**:

| 检查项 | 说明 | 严重级别 |
|--------|------|----------|
| 对齐检查 | 检查承诺与当前共识的语义相似度和关键词匹配度 | HARD_VIOLATION / SOFT_VIOLATION |
| 范围检查 | 检查承诺是否在当前阶段范围内，检测跨阶段引用 | WARNING |
| 优先级检查 | 根据阶段评估优先级合理性，检查高优先级承诺是否充分讨论 | WARNING |
| 一致性检查 | 与历史共识比较，检测矛盾和张力的演变 | HARD_VIOLATION / SOFT_VIOLATION |
| 可行性检查 | 检测不切实际的承诺模式（如"绝对"、"完美"等） | WARNING |

**新增功能**:
- 整体评分系统 (0-100)
- 跨阶段一致性分析
- 承诺链遗忘检测
- 详细问题建议生成
- 校验结果摘要自动生成

### 8.2 导出功能

**新增文件**:
- `backend/src/services/exportService.js` - 后端导出服务
- `backend/src/routes/exports.js` - 导出 API 路由
- `frontend/src/components/ExportButton.jsx` - 前端导出按钮组件

**支持格式**:

| 格式 | 文件扩展名 | 特点 |
|------|------------|------|
| Markdown | .md | 纯文本，易于编辑和版本控制 |
| HTML | .html | 可在浏览器中打开，支持 Ctrl+P 打印为 PDF |

**导出内容包括**:
- 辩论基本信息（话题、状态、时间）
- 阶段概览
- 完整辩论记录（按阶段分组）
- 阶段共识和核心承诺
- 回溯校验结果（含评分）
- 参与角色信息

### 8.3 UI/UX 优化

**文件**: `frontend/src/components/ConsensusPanel.jsx`

**新增/改进功能**:
- 可展开的回溯校验详情面板
- 整体评分可视化（带趋势图标）
- 问题统计卡片（严重问题/潜在问题数量）
- 各检查项详细展示：
  - 对齐检查：语义相似度进度条
  - 范围检查：跨阶段引用列表
  - 优先级检查：承诺级别显示
  - 一致性检查：矛盾数量统计
  - 可行性检查：不切实际模式提示
- 具体问题列表，包含代码、描述和建议
- 跨阶段问题独立展示区域
- 平滑的展开/收起动画

### 8.4 单元测试和集成测试

**后端测试文件**:
- `backend/tests/debateEngine.test.js` - DebateEngine 完整测试
  - 构造函数测试
  - start/stop/pause/resume 方法测试
  - 消息和承诺管理测试
  - 共识生成测试
  - BacktrackValidator 测试
  - ContextManager 测试
  - 5选3推进规则测试

- `backend/tests/exportService.test.js` - ExportService 测试
  - Markdown 生成测试
  - HTML 生成测试
  - 状态文本转换测试
  - 角色表情映射测试
  - 文件导出测试

**前端测试文件**:
- `frontend/src/tests/debateStore.test.jsx` - Zustand Store 测试
  - 初始状态验证
  - 状态设置方法测试
  - 数据添加方法测试
  - 重置功能测试
  - 阶段配置验证

**运行测试命令**:

```bash
# 后端测试
cd backend && bun run test

# 前端测试
cd frontend && bun run test

# 运行覆盖率报告
cd backend && bun run test:coverage
cd frontend && bun run test:coverage
```

---

## 八、附录

### 8.1 技术栈

- **前端**: React 18 + Vite + Tailwind CSS + Zustand
- **后端**: Node.js + Express + WebSocket (ws)
- **构建工具**: Bun

### 8.2 端口配置

- **前端开发服务器**: 9531
- **后端 API 服务器**: 9528
- **WebSocket 服务器**: 9528 (与 API 共享端口)

### 8.3 相关文档

- [Debate Skill 原理报告](G:\ai-gongju\prd-debate\debate\skill原理报告.md)
- [Debate Skill 使用说明](G:\ai-gongju\prd-debate\debate\SKILL.md)

---

**文档结束**
