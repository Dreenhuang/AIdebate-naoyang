# PRD 辩论系统 V2.2 升级指南

## 📋 版本信息

| 项目 | 内容 |
|------|------|
| **版本号** | V2.2 |
| **发布日期** | 2026-05-09 |
| **升级内容** | 流式输出 + 取消按钮 + 离线模式 |
| **作者** | GLM-5V-Turbo + Trae IDE |

---

## ✨ 新功能概览

### 🔥 功能一：流式输出（Stream Output）

**问题描述**：
- 旧版本：AI 回复需要等待全部生成完毕才显示
- 用户看到"AI 正在思考..."的加载动画，体验不佳

**解决方案**：
- 实现逐字/逐块实时显示 AI 的回复
- 用户可以实时看到生成过程，无需等待

**技术实现**：
```
后端: OpenAI Stream API (stream: true)
   ↓ WebSocket 实时推送
前端: React 状态管理 + 逐字渲染
```

**效果演示**：

```
[之前] 等待 10 秒 → 一次性显示完整回复

[现在] 第1秒: "我"
       第2秒: "我认为"
       第3秒: "这个方案..."
       ... 实时显示每个字
```

---

### 🛑 功能二：取消按钮（Cancel Button）

**问题描述**：
- 一旦开始讨论，无法中断
- 如果用户发现问题或想重新开始，只能等待完成或关闭页面

**解决方案**：
- 添加醒目的取消按钮（红色高亮）
- 使用 AbortController 立即终止 API 请求
- 不影响已生成的消息，只停止当前正在进行的生成

**触发时机**：
- 辩论进行中且 AI 正在生成时显示
- 点击后立即生效，无延迟

**UI 位置**：
- 主界面右上角工具栏
- 流式消息组件内部也有取消按钮

---

### 💾 功能三：离线模式（Offline Mode）

**问题描述**：
- 网络断开时，用户操作丢失
- 需要手动重试所有操作

**解决方案**：
- 自动检测网络状态变化
- 离线时自动缓存用户操作到 LocalStorage
- 网络恢复后自动重试缓存的操作
- 提供可视化的队列管理界面

**支持的离线操作**：
| 操作 | 是否支持 | 说明 |
|------|---------|------|
| 开始讨论 | ✅ | 缓存配置，恢复后自动启动 |
| 停止讨论 | ✅ | 缓存指令 |
| 重置辩论 | ✅ | 缓存指令 |
| 取消生成 | ✅ | 缓存指令 |

**存储限制**：
- 最大缓存 50 条操作
- 单条操作最大 5KB
- 超过限制时自动清理最旧的操作

---

## 📁 修改的文件清单

```
g:\ai-gongju\prd-debate\taolun-web\
│
├── backend/
│   ├── .env                              [修改] 新增超时和重试配置
│   ├── src/
│   │   ├── services/
│   │   │   └── debateEngine.js           [重大修改] 添加流式输出+取消功能
│   │   └── websocket/
│   │       └── handler.js                [修改] 添加流式事件+取消处理
│   └── test-api-connection.js            [新增] API 连接测试工具
│
└── frontend/
    ├── src/
    │   ├── components/
    │   │   ├── StreamMessage.jsx           [新增] 流式消息显示组件
    │   │   ├── OfflineIndicator.jsx        [新增] 离线状态指示器
    │   │   └── MessageStream.jsx          [修改] 集成流式显示
    │   ├── hooks/
    │   │   └── useWebSocket.js            [修改] 处理流式事件+离线缓存
    │   ├── stores/
    │   │   └── debateStore.js             [修改] 添加流式状态管理
    │   ├── utils/
    │   │   └── offlineManager.js           [新增] 离线模式管理器
    │   └── App.jsx                         [修改] 集成取消按钮+离线指示器
    └── ...
```

---

## 🚀 快速开始

### 步骤 1：安装依赖（如果需要）

```bash
cd g:\ai-gongju\prd-debate\taolun-web\backend
npm install   # 或 bun install

cd g:\ai-gongju\prd-debate\taolun-web\frontend
npm install   # 或 bun install
```

### 步骤 2：启动后端服务

```bash
cd g:\ai-gongju\prd-debate\taolun-web\backend
node src/index.js
```

**预期输出**：
```
[Server] Backend running on port 9528
[WebSocket] Server initialized
[DebateEngine] AI客户端初始化完成
[DebateEngine] API端点: https://api.deepseek.com
[DebateEngine] 超时设置: 60000ms
[DebateEngine] 最大重试: 3次
```

### 步骤 3：启动前端服务

```bash
cd g:\ai-gongju\prd-debate\taolun-web\frontend
npm run dev   # 或 bun run dev
```

### 步骤 4：打开浏览器访问

通常在 `http://localhost:5173` 或 Vite 显示的端口

---

## 🎯 功能测试清单

### 流式输出测试

- [ ] 开始一个新讨论
- [ ] 观察 AI 回复是否**逐字/逐句**出现（而不是一次性显示）
- [ ] 检查是否有**打字光标动画**
- [ ] 检查**字符计数器**是否实时更新
- [ ] 检查**波浪动画指示器**是否活跃

### 取消按钮测试

- [ ] 开始讨论，等待 AI 开始生成
- [ ] 观察右上角是否出现红色的 **"取消生成"** 按钮
- [ ] 点击取消按钮
- [ ] 确认当前生成**立即停止**
- [ ] 确认之前的消息**不受影响**
- [ ] 观察是否可以继续正常操作

### 离线模式测试

#### 测试 A：离线检测

- [ ] 断开网络（可拔掉网线或使用浏览器开发者工具）
- [ ] 观察左下角是否出现**红色离线提示框**
- [ ] 尝试点击"开始讨论"
- [ ] 确认操作被**自动缓存**（提示框显示队列数量）
- [ ] 恢复网络
- [ ] 观察提示框变为绿色，并提示"网络已恢复"
- [ ] 观察缓存的操作是否**自动执行**

#### 测试 B：队列管理

- [ ] 在离线状态下多次操作
- [ ] 点击离线提示框展开详情
- [ ] 查看**待处理操作列表**
- [ ] 尝试点击"清空全部"（确认对话框）
- [ ] 确认队列已被清空

---

## ⚙️ 配置说明

### 后端 .env 配置

```env
# ============================================
# PRD 辩论系统 - 后端环境配置 (V2.2)
# ============================================

# 服务器端口
PORT=9528

# DeepSeek AI API 配置
DEEPSEEK_API_KEY=你的API密钥
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-flash

# API 调用超时设置（毫秒）- 影响流式输出的最长等待时间
API_TIMEOUT_MS=60000

# 最大重试次数 - 连接失败时的重试次数
API_MAX_RETRIES=3
```

### 关键参数说明

| 参数 | 默认值 | 说明 | 建议 |
|------|--------|------|------|
| `API_TIMEOUT_MS` | 60000 | 单次 API 调用的最长等待时间 | 短对话可设为 30000 |
| `API_MAX_RETRIES` | 3 | 失败后的自动重试次数 | 稳定网络可设为 1-2 |

---

## 🔧 高级用法

### 自定义流式显示样式

编辑 `StreamMessage.jsx` 组件，修改以下部分：

```jsx
// 修改打字光标颜色
<span className="inline-block w-0.5 h-4 bg-brand-primary ml-0.5" />

// 修改波浪指示器颜色
<div className="w-1 bg-brand-primary/60 rounded-full" />
```

### 调整离线队列大小

编辑 `offlineManager.js`：

```javascript
const MAX_QUEUE_SIZE = 50; // 修改此值调整最大缓存数量
```

### 禁用流式输出（回退到旧版）

如果想暂时禁用流式输出，修改 `debateEngine.js`：

```javascript
// 在 executeProposer 方法中
// 将 callAIStream 改回 callAI
const content = await this.callAI(...); // 使用非流式版本
```

---

## 🐛 故障排除

### 问题 1：流式输出不工作

**症状**：仍然显示旧的加载动画

**可能原因**：
1. 后端未重启（需要加载新代码）
2. DeepSeek API 不支持流式（不太可能）
3. 浏览器控制台有错误

**解决步骤**：
```bash
# 1. 重启后端服务
Ctrl+C  # 停止当前运行的服务
node src/index.js  # 重新启动

# 2. 检查浏览器控制台（F12）是否有错误
# 3. 刷新页面（Ctrl+F5 强制刷新）
```

### 问题 2：取消按钮无效

**症状**：点击取消但没有反应

**可能原因**：
1. 当前没有正在进行的生成（按钮只在生成时显示）
2. WebSocket 连接断开

**解决步骤**：
1. 确认 AI 正在生成（看到流式文字出现）
2. 确认右上角显示红色"取消生成"按钮
3. 检查浏览器控制台是否有错误信息

### 问题 3：离线模式不缓存操作

**症状**：断网后点击按钮没有任何反应

**可能原因**：
1. LocalStorage 被禁用或满
2. 浏览器隐私模式

**解决步骤**：
1. 检查浏览器是否允许使用 LocalStorage
2. 打开 F12 → Application → Local Storage，查看是否有数据
3. 尝试在普通模式下使用（非隐私模式）

---

## 📊 性能对比

| 指标 | V2.1（旧版） | V2.2（新版） | 提升 |
|------|-------------|-------------|------|
| **首字显示时间** | ~10秒（等全部生成完） | <1秒 | **90%↑** |
| **用户感知延迟** | 高（必须等待） | 低（即时反馈） | **显著改善** |
| **资源占用** | 低（单次请求） | 中（持续连接） | 可接受 |
| **网络容错性** | 差（断开即失败） | 优秀（自动缓存） | **质的飞跃** |
| **用户控制力** | 弱（无法中断） | 强（随时取消） | **完全掌控** |

---

## 🎓 技术原理说明

### 流式输出原理

```
传统方式:
Client ──请求──> Server ──(等待10s)──> 返回完整响应
                     ↑
                  全部生成完毕才能返回

流式方式:
Client ──请求──> Server
         ←── chunk1 ──  (第1个token)
         ←── chunk2 ──  (第2个token)
         ←── chunk3 ──  (第3个token)
         ...
         ←── done ─────  (结束标记)
```

**优势**：
- 用户首字等待时间从 10s 降到 <1s
- 可以提前判断内容质量，不满意可及时取消
- 更自然的阅读体验（像真人打字）

### AbortController 原理

```javascript
// 创建控制器
const controller = new AbortController();

// 设置超时自动取消
setTimeout(() => controller.abort(), 60000);

// 传入 fetch/Axios 请求
fetch(url, { signal: controller.signal });

// 手动取消
controller.abort(); // 立即中断请求，抛出 AbortError
```

**应用场景**：
- 用户主动取消
- 超时自动取消
- 组件卸载时清理

### 离线优先策略（Offline First）

```
发送操作时:
  if (在线) {
    直接发送到服务器
  } else {
    存入 LocalStorage 队列
    显示"已缓存"提示
  }

网络恢复时:
  1. 监听 window.online 事件
  2. 遍历队列中的操作
  3. 逐个重新发送
  4. 成功则移除，失败则保留（最多重试3次）
```

---

## 📝 更新日志

### V2.2 (2026-05-09)

#### 新增功能
- ✅ 流式输出（Stream Output）：AI 回复逐字实时显示
- ✅ 取消按钮（Cancel Button）：随时中断当前生成
- ✅ 离线模式（Offline Mode）：断网自动缓存，恢复自动重试

#### 改进功能
- 🔄 优化 API 错误处理（分类错误类型 + 提供建议）
- 🔄 添加智能重试机制（指数退避算法）
- 🔄 增强日志输出（便于调试）

#### 修复问题
- 🐛 修复"卡在 AI 正在思考..."的问题
- 🐛 修复无超时保护导致的永久等待

---

## 💡 使用技巧

### 技巧 1：快速预判质量

利用流式输出特性，观察前几句内容：
- 如果明显跑题，立即取消重新开始
- 无需等待完整生成浪费时间

### 技巧 2：节省 API 额度

对于简单问题，发现答案正确后即可取消：
- 流式输出已显示足够信息
- 取消剩余生成节省 token

### 技巧 3：弱网环境使用

在网络不稳定的环境（如移动热点、咖啡厅 WiFi）：
- 系统会自动缓存您的操作
- 不必担心因网络波动丢失工作
- 恢复网络后一切自动继续

---

## 🆘 获取帮助

如果遇到问题：

1. **查看本文档**的故障排除章节
2. **检查浏览器控制台**（F12）的错误信息
3. **查看后端终端**的日志输出
4. **运行 API 测试**确认连接正常：
   ```bash
   cd backend
   node test-api-connection.js
   ```

---

## 📄 许可证

本项目基于 MIT 许可证开源。

---

**🎉 感谢使用 PRD 辩论系统 V2.2！**

如有任何建议或问题，欢迎反馈！
